# SQL / data

DuckDB integration — SQL nodes over Polars frames. Requires the `sql` extra (`pip install "golit[sql]"`); DuckDB is imported lazily, never at framework import time. See the [SQL nodes](../tutorial/sql.md) tutorial.

::: golit.data
    options:
      members_order: source
      show_root_heading: false
      show_root_toc_entry: false
