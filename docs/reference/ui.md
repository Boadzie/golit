# UI components

Presentational, shadcn-styled, server-rendered builders. Each returns an HTML string and runs its arguments through Golit's renderer, so components nest with DataFrames, charts, and each other. See the [UI components](../tutorial/ui-components.md) tutorial.

::: golit.ui
    options:
      members_order: source
      show_root_heading: false
      show_root_toc_entry: false
